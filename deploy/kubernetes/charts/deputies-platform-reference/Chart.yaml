apiVersion: v2
name: deputies-platform-reference
description: Reference-only platform services for a Deputies Kubernetes deployment.
type: application
version: 0.1.0
appVersion: '0.1.0'

dependencies:
  - name: traefik
    version: '~40.2.0'
    repository: https://traefik.github.io/charts
    condition: traefik.enabled
